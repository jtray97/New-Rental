module.exports={
    users:function(req, res){

    },
    quads:function(req, res){
        const db = req.app.get('db')
        const {name, description, owner_id, ppd, zip_code, img1, img2, img3, img4}
    },
    rvs:function(req, res){
        const db = req.app.get('db')
        const {name, description, owner_id, ppd, zip_code, img1, img2, img3, img4}
    },
    utvs:function(req, res){
        const db = req.app.get('db')
        const {name, description, owner_id, ppd, zip_code, img1, img2, img3, img4}
    },
    jetskis:function(req, res){
        const db = req.app.get('db')
        const {name, description, owner_id, ppd, zip_code, img1, img2, img3, img4}
    },
    boats:function(req, res){
        const db = req.app.get('db')
        const {name, description, owner_id, ppd, zip_code, img1, img2, img3, img4}
    },
    campers:function(req, res){
        const db = req.app.get('db')
        const {name, description, owner_id, ppd, zip_code, img1, img2, img3, img4}
    },
    packages:function(req, res){
        const db = req.app.get('db')
        const {name, description, owner_id, ppd, zip_code, img1, img2, img3, img4}
    },
    others:function(req, res){
        const db = req.app.get('db')
        const {name, description, owner_id, ppd, zip_code, img1, img2, img3, img4}
    }
}